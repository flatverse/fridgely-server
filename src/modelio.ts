import {FridgeState} from "@flatverse/fridgely-model" // why full resolved path needed????

export async function readModelFile(path:string):Promise<FridgeState> {
  console.log("HERE")
  throw Error("OK")
}
